# Mission: 월별 멤버십 쿠폰 배치의 안전한 반영과 운영 검수

## Why
매달 반복되는 멤버십 쿠폰 작업에서 티켓 요구값을 코드와 대조하고, 안전장치 SP 2개만 반영한 뒤 기존 월별 객체와 마스터까지 실제 발급 없이 판정한다. 장애를 막는 차단 오류와 단순 배포 실패를 구분해 정규 배치 전에 문제를 발견한다.

## Success looks like
- 티켓의 EventId·NType·만료일을 관련 함수와 SP에 정확히 매핑한다.
- dev 반영 전 마스터 데이터와 롤백 수단을 확인한다.
- 읽기 전용 프리플라이트를 실행하고 오류 번호와 데이터 무변경성을 판독한다.
- 통합 검수의 종료 코드 0과 최종 PASS 6개를 확인한다.
- 정상 통과와 의도된 차단을 구분해 다음 행동을 결정한다.

## Constraints
- 운영 DB에서는 승인된 시간에 읽기 전용 통합 검수만 실행한다.
- 실제 쿠폰 발급과 회원 등급 변경 경로는 테스트하지 않는다.
- 기존 배치와 SSIS 파라미터 계약을 유지한다.
- 안전장치 SP 2개만 선택 반영하며 기존 월별 객체 3개는 우선 검수한다.

## Out of scope
- 승인되지 않은 운영 배치 실행과 대량 쿠폰 발급
- CouponNType·Event 마스터 데이터 생성 절차
- SSIS 패키지 변경과 SQL Agent 스케줄 변경
