# {{서비스명}} — AGENTS.md

> 이 파일은 AI와 개발자 모두를 위한 서비스 작업 가이드입니다.
> 작업 전에 읽고, 작업 후 새로 알게 된 운영 지식을 갱신한다 (기본 — 갱신 대상은 harness-guide §하네스 갱신 트리거).

## 서비스 개요

- **목적**: {{서비스 목적}}
- **Owner**: {{담당자}}
- **Backup**: {{백업 담당자}}
- **YouTrack 프로젝트**: {{프로젝트 키}}

## 빌드/실행

```bash
# 빌드
{{빌드 명령}}

# 테스트
{{테스트 명령}}

# 로컬 실행
{{실행 명령}}

# lint/format
{{린트 명령}}
```

## 기술 스택

- 언어: {{언어}}
- 프레임워크: {{프레임워크}}
- DB: {{DB 종류 및 인스턴스}}
- 배포: {{배포 방식 — IIS / K8s / Vercel 등}}

## 금지 사항

> 아래 항목은 절대 수정하지 마세요. 위반 시 운영 장애 위험이 있습니다.

- {{금지 사항 1 — 예: usp_SaveOrder SP 직접 수정 금지}}
- {{금지 사항 2 — 예: ORDER_MASTER 테이블 스키마 변경 금지}}
- {{금지 사항 3}}

## 주요 엔드포인트

| 경로 | 메서드 | 설명 | 비고 |
|------|--------|------|------|
| {{/api/...}} | {{GET/POST}} | {{설명}} | {{특이사항}} |

## 데이터

- **Main DB**: {{DB명}}
- **Write Owner 테이블**:
  - {{테이블1}}
  - {{테이블2}}
- **관련 SP**:
  - `{{SP명1}}` — {{용도, 주의사항}}
  - `{{SP명2}}` — {{용도, 주의사항}}

## 의존 시스템

### Inbound (이 서비스를 호출하는 곳)
- {{시스템/서비스명}} — {{호출 방식}}

### Outbound (이 서비스가 호출하는 곳)
- {{시스템/서비스명}} — {{호출 방식}}

## 배포/롤백

- **배포 방법**: {{배포 절차 요약}}
- **롤백 방법**: {{롤백 절차 요약}}
- **배포 후 확인 포인트**:
  - {{확인 항목 1}}
  - {{확인 항목 2}}

## Smoke Test

구현 중 실제 동작 검증 계약은 팀 카탈로그 `verification.runtime_contract`로 찾고, 없으면 기존 검증 루프와 제약을 따른다. 실제 계약은 서비스 repo가 소유하고, 생성 형식은 팀 하네스 `templates/service-harness/VERIFICATION.md.tmpl`이다.

```bash
# 배포 후 최소 검증
{{smoke test 명령 또는 시나리오}}
```

## 주의사항 (계속 갱신)

> 코드·검사로 표현하기 어려운 외부 제약과 결정 이유만 참조 링크로 남긴다. 사건 이력은 운영 노트에 두고, 반복 오류는 하네스 감사의 도구화 후보로 보낸다.

- {{날짜}} — {{주의사항 내용}}

## 현대화

- **트랙**: {{Observe / Wrap / Extract / Freeze/Retire}} — 트랙 정의는 팀 하네스 `policies/legacy-modernization-policy.md` §현대화 4트랙
- **상세**: `modernization-plan.md` 참조
