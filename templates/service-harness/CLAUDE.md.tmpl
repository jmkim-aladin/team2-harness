# CLAUDE.md — {{서비스명}}

This file provides guidance to Claude Code when working with code in this repository.

## 팀 하네스

개발 2팀 하네스: `$TEAM2_HARNESS_PATH/policies/` (정책), `$TEAM2_HARNESS_PATH/catalog/{{서비스ID}}.yaml` (서비스 프로파일)
브랜치: `feature/{이슈ID}` | 커밋: `[{이슈ID}] 작업 내용`

## Project Overview

{{서비스 설명}}

- **Stack**: {{기술 스택}}
- **DB**: {{DB 정보}}
- **Deploy**: {{배포 방식}}

## Build & Development

```bash
{{빌드/테스트/실행 명령}}
```

## 실행 검증 계약

팀 카탈로그 `verification.runtime_contract`가 가리키는 서비스 repo의 계약을 읽는다. 계약이 없으면 기존 빠른 검증 루프와 환경 제약을 따르고, 생성·실행·등록은 `$TEAM2_HARNESS_PATH/docs/service-harness-setup.md`의 실행 검증 계약 연결 절차를 따른다.

## Key Structure

```
{{주요 디렉토리 구조}}
```
