# 알라딘 쇼핑 멤버십 쿠폰 재발방지 월간 이행 보고서 템플릿

> 2026-05 커피 쿠폰 대상월 오류와 2026-08 멤버십 쿠폰 만료일 오류의 재발방지 약속을 월별로 검증한다. 새 장애가 없더라도 안전장치 운영 반영 후 3회 연속 정상 확인까지 작성한다.

## 1. 회차 정보

| 항목 | 내용 |
| --- | --- |
| 대상월 | `{{YYYYMM}}` |
| 요청 티켓 | `{{DEV2-XXXX}}` |
| 개발·안전장치 티켓 | `{{DEV2-XXXX}}` |
| 예정 발급 시각 | `{{YYYY-MM-DD HH:mm KST}}` |
| 실제 발급 시각 | `{{YYYY-MM-DD HH:mm KST 또는 미실행}}` |
| 작업자 / 검토자 / 관리자 테스트 확인자 / D+1 확인자 | `{{이름}} / {{이름}} / {{이름}} / {{이름}}` |
| 보고 판정 | `정상 / 조건부 / 미완료 / 장애` |

### 판정 기준

- `정상`: 운영 Preflight, 관리자 테스트, 본 발급, 사후 데이터, D+1 업무 확인까지 모두 통과
- `조건부`: 발급은 완료했으나 독립 운영 조회나 업무 확인 일부가 남음
- `미완료`: 안전장치 미배포, Preflight 미통과, 본 발급 미실행 중 하나 이상
- `장애`: 잘못된 설정의 발급 진입, 잘못된 결과 생성, 고객 영향 중 하나 이상 발생

## 2. 요청값 기준선

| 구분 | 기대값 | 확인 근거 | 결과 |
| --- | --- | --- | --- |
| 대상월 | `{{YYYYMM}}` | 요청 티켓 | `PASS/FAIL` |
| 멤버십 EventId | `{{EventId}}` | 요청 티켓·마스터 조회 | `PASS/FAIL` |
| 멤버십 NType 10종 | `{{목록}}` | 요청 티켓·마스터 조회 | `PASS/FAIL` |
| 모바일 NType | `{{NType}}` | 요청 티켓·예외 설정 | `PASS/FAIL` |
| 커피 EventId / NType | `{{EventId}} / {{NType}}` | 요청 티켓·마스터·월 매핑 | `PASS/FAIL` |
| 만료일 | `{{YYYY-MM-DD 23:59:00}}` | 요청값·발급 후보 | `PASS/FAIL` |

## 3. 재발방지 약속 이행

| 보고한 약속 | 이번 회차 결과 | 증거 | 판정 |
| --- | --- | --- | --- |
| 대상월·쿠폰 타입·만료일·누락·중복 자동 검증 | `{{결과}}` | `{{Preflight 출력/오류 코드}}` | `완료/부분/미이행/확인불가` |
| 검증 실패 시 실제 발급 전 중단 | `{{결과}}` | `{{0건·쿠폰번호 무변경}}` | `완료/부분/미이행/확인불가` |
| 실패 알림과 자동 재실행 금지 | `{{결과}}` | `{{Job 상태·알림 수신}}` | `완료/부분/미이행/확인불가` |
| 배치 직후 타입별 건수·만료일 확인 | `{{결과}}` | `{{운영 조회 결과}}` | `완료/부분/미이행/확인불가` |
| D+1 사용자 노출·사용 가능 확인 | `{{결과}}` | `{{확인자·시각·결과}}` | `완료/부분/미이행/확인불가` |
| 검증된 처리만 단계 전환·독립 복구 | `{{결과}}` | `{{PR·승인·롤백 절차}}` | `완료/부분/미이행/확인불가` |
| 운영 반영 범위 3회 연속 정상 추적 | `{{N}}/3` | `{{월별 보고서 링크}}` | `진행/완료/재시작` |
| REF-A-221 운영 절차 갱신 | `{{결과}}` | `{{KB 변경 이력}}` | `완료/미이행` |

## 4. 실행 결과

### 4.1 운영 반영

- 승인자·승인 시각: `{{내용}}`
- 반영 객체: `{{객체 목록}}`
- 변경 전 정의 보관: `{{위치 또는 증거}}`
- Smoke test: `{{결과}}`
- 롤백 준비: `{{결과}}`

### 4.2 발급 전 Preflight

- 실행 시각·실행자: `{{내용}}`
- 종료 코드: `{{0 또는 오류 코드}}`
- 결과: `{{PASS 항목 수}}`
- 발급 데이터 변경: `{{0건}}`
- 실패 시 조치: `{{본 발급 중단·설정 수정·재검증}}`

### 4.3 관리자 테스트 확인

- 테스트 시각·테스트 계정: `{{내용}}`
- 테스트 발급 NType: `{{목록}}`
- 발급 데이터: `{{건수·만료일·중복 결과}}`
- 관리자 화면 노출: `{{정상/오류}}`
- 다운로드·사용 가능 여부: `{{정상/오류}}`
- 확인자·확인 댓글: `{{내용}}`
- 판정: `{{통과/본 발급 중단}}`

### 4.4 본 발급

- 실행 주체: `{{SQL Agent/수동 승인 실행}}`
- Job 상태: `{{성공/실패/확인불가}}`
- 총 발급 건수: `{{건수}}`
- 자동 재실행: `{{없음/있음과 사유}}`

### 4.5 사후 데이터 검증

| 검증 | 결과 | 증거 |
| --- | --- | --- |
| NType별 발급 건수 | `{{결과}}` | `{{조회 결과}}` |
| 만료일 최소·최대 | `{{결과}}` | `{{조회 결과}}` |
| 대상월 밖 만료일 | `0건` | `{{조회 결과}}` |
| 동일 고객·NType 중복 | `0건` | `{{조회 결과}}` |
| 오류 로그·Job 재시도 | `{{결과}}` | `{{로그/Job 이력}}` |

### 4.6 D+1 업무 확인

- 확인 시각·확인자: `{{내용}}`
- PC·모바일 노출: `{{정상/오류}}`
- 커피 쿠폰 조회·다운로드: `{{정상/오류}}`
- 사용자 사용 가능 여부: `{{정상/오류}}`
- 이슈·추가 조치: `{{내용}}`

## 5. 증거 수준과 미확인 항목

- 운영자 댓글은 실행 보고 근거로 사용하지만 DB 실측과 동일하게 취급하지 않는다.
- 운영 조회 결과에는 실행 시각, 대상월, 쿼리 목적, 집계값을 남긴다. 고객 식별 row는 보고서에 남기지 않는다.
- 확인하지 못한 항목은 `정상`으로 추정하지 않고 `확인불가`로 표시한다.
- 발급이 이미 끝난 회차는 재검증 목적으로 다시 발급하지 않는다. 읽기 전용 조회와 업무 확인만 수행한다.

## 6. 결론과 다음 회차

- 이번 회차 판정: `{{정상/조건부/미완료/장애}}`
- 3회 안정화 누적: `{{N}}/3`
- 미완료 약속: `{{목록}}`
- 다음 회차 전 필수 조치: `{{목록}}`
- 다음 회차 예정일: `{{YYYY-MM-DD}}`

## 7. 근거

- 원 장애: `DEV2-5999`, `DEV2-7810`
- 이번 회차 요청: `{{DEV2-XXXX}}`
- 안전장치 개발: `DEV2-8924` 및 하위 Task
- 코드·검수 SQL: `shop-db-script`
- 운영 절차: `membership-monthly-precheck-checklist`
